{
  "name": "phaser-plugin-pixelscale",
  "version": "0.1.0",
  "description": "WebGL post-FX pipeline for Phaser 3 that snaps any scene to a virtual pixel grid, with optional palette quantisation, to render games as pixel art without authoring at a tiny canvas resolution.",
  "main": "dist/phaser-plugin-pixelscale.umd.js",
  "module": "dist/phaser-plugin-pixelscale.esm.js",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/phaser-plugin-pixelscale.esm.js",
      "require": "./dist/phaser-plugin-pixelscale.umd.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "phaser",
    "phaser3",
    "plugin",
    "pixel-art",
    "pixelscale",
    "shader",
    "webgl",
    "postfx",
    "pipeline",
    "retro"
  ],
  "peerDependencies": {
    "phaser": ">=3.50.0"
  },
  "devDependencies": {
    "rollup": "^4.0.0",
    "@rollup/plugin-terser": "^0.4.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YOUR_GITHUB_USER/phaser-plugin-pixelscale.git"
  }
}
